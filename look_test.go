package main

/*
type lookTest struct{
	q0, q1 int64 // pre-sweep selection
	s0, s1 int64 // post-sweep selection
	data string
	
	t0, t1 // post-look selection
}

var testText = "the quick brown fox jumps over the lazy dog"

func TestLook(t *testing.T){
	l := []lookTest{
		{0, 0,	1,1	testText, 31,33},
	}
	for i,v := range l{
		buf := text.NewEditor(text.BufferFrom([]byte(testText)))
		buf.
	}
	
}
	*/